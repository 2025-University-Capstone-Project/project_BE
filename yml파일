aws:
  access:
    key:
      id: ${AWS_ACCESS_KEY_ID}  # AWS Access Key ID (환경변수 사용 권장)
  secret:
    access:
      key: ${AWS_SECRET_ACCESS_KEY}  # AWS Secret Access Key (환경변수 사용 권장)
  s3:
    bucket:
      name: "baseball-web-project"  # S3 버킷 이름
  region: "ap-northeast-2"  # AWS 리전 (서울 리전)
